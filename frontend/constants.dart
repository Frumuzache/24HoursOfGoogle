import 'package:flutter/material.dart';

class AppColors {
  // --- THE BLUES (The Foundation) ---
  static const Color bgMist = Color(0xFFF4F7F9);      // Main background (Morning Mist)
  static const Color surfaceBlue = Color(0xFFE1EAF4); // Card backgrounds
  static const Color deepSerenity = Color(0xFF5D8AA8); // Primary actions (Trust)
  static const Color midnightText = Color(0xFF1B263B); // Headers (The "Anchor" color)

  // --- THE GREENS (The Healing) ---
  static const Color sageGrounding = Color(0xFFB8C7B1); // AI chat bubbles / Healthy status
  static const Color forestQuiet = Color(0xFF4A5D4E);   // Success text / Secondary buttons

  // --- THE REDS/WARMS (The Human Touch) ---
  static const Color softAwareness = Color(0xFFE8998D); // High BPM / Alert state
  static const Color sunsetComfort = Color(0xFFF4D35E); // Tips / Medication reminders
}