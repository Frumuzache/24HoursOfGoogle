//color scheme constants
import 'package:flutter/material.dart';

class AppColors {
  // --- blue scheme (Vibrant & Trustworthy) ---
  static const Color bgMist = Color(0xFFD5F6FA);      // Light & Airy
  static const Color surfaceBlue = Color(0xFFABF0FA); // Clear contrast
  static const Color deepSerenity = Color(0xFF3268FA); // Action Blue
  static const Color midnightText = Color(0xFF2844DC); // Deep Anchor

  // --- green scheme (Healthy & Positive) ---
  static const Color sageGrounding = Color(0xFF6FE37B); // High-vis Green
  static const Color forestQuiet = Color(0xFF0E6F00);   // Dark Success

  // --- red scheme (Urgent & Noticable) ---
  static const Color softAwareness = Color(0xFFE80033); // Alert Red
  static const Color sunsetComfort = Color(0xFFE85933); // Warning Orange
}